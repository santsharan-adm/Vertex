<UserControl x:Class="IPCSoftware.UI.CommonViews.CcdSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920"
             Background="#F3F3F3">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <Style x:Key="HmiLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="HmiTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="60"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="2"/>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="40"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="4" Opacity="0.1" BlurRadius="20"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Viewbox Stretch="Uniform">
        <Grid Width="1920" Height="1080">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" 
                       Text="{Binding Title}" 
                       FontSize="48" 
                       FontWeight="Bold"
                       Foreground="#333333"
                       Margin="40,30,0,20"/>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" PanningMode="VerticalOnly" Margin="40,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Text="CCD Primary Settings" FontSize="32" FontWeight="Bold" Margin="0,0,0,30"/>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="30"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Left column -->
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="QrCodeImagePath" Style="{StaticResource HmiLabelStyle}"/>
                                        <TextBox Text="{Binding QrCodeImagePath, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>

                                        <TextBlock Text="TempImgFolder" Style="{StaticResource HmiLabelStyle}" Margin="0,20,0,0"/>
                                        <TextBox Text="{Binding TempImgFolder, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>

                                        <TextBlock Text="ImageRootFolder" Style="{StaticResource HmiLabelStyle}" Margin="0,20,0,0"/>
                                        <TextBox Text="{Binding ImageRootFolder, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>

                                        <TextBlock Text="MetadataStyle" Style="{StaticResource HmiLabelStyle}" Margin="0,20,0,0"/>
                                        <TextBox Text="{Binding MetadataStyle, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                    </StackPanel>

                                    <!-- Right column -->
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="CurrentCycleStateFileName" Style="{StaticResource HmiLabelStyle}"/>
                                        <TextBox Text="{Binding CurrentCycleStateFileName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>

                                        <TextBlock Text="(Client) Metadata - summary" FontSize="20" FontWeight="SemiBold" Foreground="#333333" Margin="0,30,0,10"/>

                                        <!-- Client metadata fields - placed in two-column grid inside this column -->
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="10"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <StackPanel Grid.Row="0" Grid.Column="0">
                                                <TextBlock Text="Version" Style="{StaticResource HmiLabelStyle}"/>
                                                <TextBox Text="{Binding Client_Version, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                            </StackPanel>
                                            <StackPanel Grid.Row="0" Grid.Column="2">
                                                <TextBlock Text="Date" Style="{StaticResource HmiLabelStyle}"/>
                                                <TextBox Text="{Binding Client_Date, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                            </StackPanel>

                                            <StackPanel Grid.Row="1" Grid.Column="0">
                                                <TextBlock Text="Time" Style="{StaticResource HmiLabelStyle}"/>
                                                <TextBox Text="{Binding Client_Time, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                            </StackPanel>
                                            <StackPanel Grid.Row="1" Grid.Column="2">
                                                <TextBlock Text="VisionVendor" Style="{StaticResource HmiLabelStyle}"/>
                                                <TextBox Text="{Binding Client_VisionVendor, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                            </StackPanel>

                                            <StackPanel Grid.Row="2" Grid.Column="0">
                                                <TextBlock Text="StationID" Style="{StaticResource HmiLabelStyle}"/>
                                                <TextBox Text="{Binding Client_StationID, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                            </StackPanel>
                                            <StackPanel Grid.Row="2" Grid.Column="2">
                                                <TextBlock Text="StationNickname" Style="{StaticResource HmiLabelStyle}"/>
                                                <TextBox Text="{Binding Client_StationNickname, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                            </StackPanel>

                                            <StackPanel Grid.Row="3" Grid.Column="0">
                                                <TextBlock Text="DUTSerialNumber" Style="{StaticResource HmiLabelStyle}"/>
                                                <TextBox Text="{Binding Client_DUTSerialNumber, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                            </StackPanel>
                                            <StackPanel Grid.Row="3" Grid.Column="2">
                                                <TextBlock Text="ProcessCommand" Style="{StaticResource HmiLabelStyle}"/>
                                                <TextBox Text="{Binding Client_ProcessCommand, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                            </StackPanel>

                                            <StackPanel Grid.Row="4" Grid.Column="0">
                                                <TextBlock Text="CameraNumber" Style="{StaticResource HmiLabelStyle}"/>
                                                <TextBox Text="{Binding Client_CameraNumber, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                            </StackPanel>
                                            <StackPanel Grid.Row="4" Grid.Column="2">
                                                <TextBlock Text="XPixelSizeMM" Style="{StaticResource HmiLabelStyle}"/>
                                                <TextBox Text="{Binding Client_XPixelSizeMM, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                            </StackPanel>

                                            <StackPanel Grid.Row="5" Grid.Column="0">
                                                <TextBlock Text="YPixelSizeMM" Style="{StaticResource HmiLabelStyle}"/>
                                                <TextBox Text="{Binding Client_YPixelSizeMM, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                            </StackPanel>
                                            <StackPanel Grid.Row="5" Grid.Column="2">
                                                <TextBlock Text="CameraGain" Style="{StaticResource HmiLabelStyle}"/>
                                                <TextBox Text="{Binding Client_CameraGain, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                            </StackPanel>

                                            <StackPanel Grid.Row="6" Grid.Column="0">
                                                <TextBlock Text="CameraExposure" Style="{StaticResource HmiLabelStyle}"/>
                                                <TextBox Text="{Binding Client_CameraExposure, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                            </StackPanel>
                                            <StackPanel Grid.Row="6" Grid.Column="2">
                                                <TextBlock Text="NumberOfLightSettings" Style="{StaticResource HmiLabelStyle}"/>
                                                <TextBox Text="{Binding Client_NumberOfLightSettings, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                            </StackPanel>

                                            <StackPanel Grid.Row="7" Grid.Column="0">
                                                <TextBlock Text="LightSetting1" Style="{StaticResource HmiLabelStyle}"/>
                                                <TextBox Text="{Binding Client_LightSetting1, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                            </StackPanel>
                                            <StackPanel Grid.Row="7" Grid.Column="2">
                                                <TextBlock Text="LightSettingN" Style="{StaticResource HmiLabelStyle}"/>
                                                <TextBox Text="{Binding Client_LightSettingN, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                            </StackPanel>

                                            <StackPanel Grid.Row="8" Grid.Column="0">
                                                <TextBlock Text="DUTColor" Style="{StaticResource HmiLabelStyle}"/>
                                                <TextBox Text="{Binding Client_DUTColor, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                            </StackPanel>
                                            <StackPanel Grid.Row="8" Grid.Column="2">
                                                <TextBlock Text="ImageNickname" Style="{StaticResource HmiLabelStyle}"/>
                                                <TextBox Text="{Binding Client_ImageNickname, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                            </StackPanel>
                                        </Grid>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Text="Vendor Metadata Parameters" FontSize="32" FontWeight="Bold" Margin="0,0,0,30"/>

                                <!-- Vendor metadata rendered in two columns similar to client -->
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="30"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Version" Style="{StaticResource HmiLabelStyle}"/>
                                        <TextBox Text="{Binding Vendor_Version, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>

                                        <TextBlock Text="Date" Style="{StaticResource HmiLabelStyle}" Margin="0,20,0,0"/>
                                        <TextBox Text="{Binding Vendor_Date, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>

                                        <TextBlock Text="Time" Style="{StaticResource HmiLabelStyle}" Margin="0,20,0,0"/>
                                        <TextBox Text="{Binding Vendor_Time, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>

                                        <TextBlock Text="VisionVendor" Style="{StaticResource HmiLabelStyle}" Margin="0,20,0,0"/>
                                        <TextBox Text="{Binding Vendor_VisionVendor, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="StationID" Style="{StaticResource HmiLabelStyle}"/>
                                        <TextBox Text="{Binding Vendor_StationID, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>

                                        <TextBlock Text="StationNickname" Style="{StaticResource HmiLabelStyle}" Margin="0,20,0,0"/>
                                        <TextBox Text="{Binding Vendor_StationNickname, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>

                                        <TextBlock Text="DUTSerialNumber" Style="{StaticResource HmiLabelStyle}" Margin="0,20,0,0"/>
                                        <TextBox Text="{Binding Vendor_DUTSerialNumber, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>

                                        <TextBlock Text="ProcessCommand" Style="{StaticResource HmiLabelStyle}" Margin="0,20,0,0"/>
                                        <TextBox Text="{Binding Vendor_ProcessCommand, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                    </StackPanel>
                                </Grid>

                                <!-- Continue with remaining vendor fields in a compact grid -->
                                <Grid Margin="0,30,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="30"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="CameraNumber" Style="{StaticResource HmiLabelStyle}"/>
                                        <TextBox Text="{Binding Vendor_CameraNumber, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>

                                        <TextBlock Text="XPixelSizeMM" Style="{StaticResource HmiLabelStyle}" Margin="0,20,0,0"/>
                                        <TextBox Text="{Binding Vendor_XPixelSizeMM, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>

                                        <TextBlock Text="YPixelSizeMM" Style="{StaticResource HmiLabelStyle}" Margin="0,20,0,0"/>
                                        <TextBox Text="{Binding Vendor_YPixelSizeMM, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="CameraGain" Style="{StaticResource HmiLabelStyle}"/>
                                        <TextBox Text="{Binding Vendor_CameraGain, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>

                                        <TextBlock Text="CameraExposure" Style="{StaticResource HmiLabelStyle}" Margin="0,20,0,0"/>
                                        <TextBox Text="{Binding Vendor_CameraExposure, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>

                                        <TextBlock Text="NumberOfLightSettings" Style="{StaticResource HmiLabelStyle}" Margin="0,20,0,0"/>
                                        <TextBox Text="{Binding Vendor_NumberOfLightSettings, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                    </StackPanel>
                                </Grid>

                                <Grid Margin="0,30,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="30"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="LightSetting1" Style="{StaticResource HmiLabelStyle}"/>
                                        <TextBox Text="{Binding Vendor_LightSetting1, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>

                                        <TextBlock Text="LightSettingN" Style="{StaticResource HmiLabelStyle}" Margin="0,20,0,0"/>
                                        <TextBox Text="{Binding Vendor_LightSettingN, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="DUTColor" Style="{StaticResource HmiLabelStyle}"/>
                                        <TextBox Text="{Binding Vendor_DUTColor, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>

                                        <TextBlock Text="ImageNickname" Style="{StaticResource HmiLabelStyle}" Margin="0,20,0,0"/>
                                        <TextBox Text="{Binding Vendor_ImageNickname, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource HmiTextBoxStyle}"/>
                                    </StackPanel>
                                </Grid>

                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </ScrollViewer>

            <Border Grid.Row="2" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="30">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel"
                            Command="{Binding CancelCommand}"
                            Width="200" Height="70"
                            FontSize="24" FontWeight="SemiBold"
                            Background="#F5F5F5" Foreground="#555555"
                            BorderBrush="#CCCCCC" BorderThickness="2"
                            Margin="0,0,30,0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Button Content="Save Settings"
                            Command="{Binding SaveCommand}"
                            Width="260" Height="70"
                            FontSize="24" FontWeight="Bold"
                            Background="#007ACC" Foreground="White"
                            BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Viewbox>
</UserControl>