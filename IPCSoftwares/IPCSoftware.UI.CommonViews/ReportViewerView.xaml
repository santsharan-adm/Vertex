<UserControl x:Class="IPCSoftware.UI.CommonViews.ReportViewerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IPCSoftware.UI.CommonViews"
            mc:Ignorable="d"
             d:DesignHeight="1080" d:DesignWidth="1920"
             Background="#F5F7FA">
    <!-- Modern light gray background -->

    <UserControl.Resources>
        <!-- Modern Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="4" Opacity="0.05"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Input Label Style -->
        <Style x:Key="HmiLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="HmiComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#F9FAFB"/>
        </Style>

        <!-- Custom Thick ScrollBar for Touch -->
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Width" Value="30"/>
                </Trigger>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Height" Value="30"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <!-- 
        REMOVED Viewbox and hardcoded Width/Height.
        This Grid will now fluidly stretch to 100% of the screen.
    -->
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="Auto"/>
            <!-- Filters -->
            <RowDefinition Height="*"/>
            <!-- DataGrid (Takes remaining space) -->
            <RowDefinition Height="Auto"/>
            <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- 1. Header -->
        <TextBlock Grid.Row="0" 
                   Text="Report Viewer" 
                   FontSize="42" 
                   FontWeight="Bold"
                   Foreground="#111827"
                   Margin="0,0,0,25"/>

        <!-- 2. Filter Panel -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="25" Margin="0,0,0,25">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <!-- Pushes buttons to the right -->
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Report Format Selection -->
                <StackPanel Grid.Column="0" Orientation="Vertical" Margin="0,0,30,0">
                    <TextBlock Text="Report Format" Style="{StaticResource HmiLabelStyle}"/>
                    <StackPanel Orientation="Horizontal">
                        <ComboBox ItemsSource="{Binding ReportFormats}"
                                  SelectedItem="{Binding SelectedReportFormat}"
                                  DisplayMemberPath="Name"
                                  Style="{StaticResource HmiComboBoxStyle}"
                                  Width="300"/>

                        <Button Content="Refresh" Command="{Binding RefreshFormatsCommand}"
                                ToolTip="Refresh Report Formats"
                                Width="100" Height="50" FontSize="18"
                                Background="#E5E7EB" Foreground="#374151"
                                BorderThickness="0" Margin="10,0,0,0" Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </Button.Resources>
                            <!--<TextBlock Text="↻" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>-->
                        </Button>
                    </StackPanel>
                </StackPanel>

                <!-- Date From -->
                <StackPanel Grid.Column="1" Orientation="Vertical" Margin="0,0,30,0">
                    <TextBlock Text="From Date" Style="{StaticResource HmiLabelStyle}"/>
                    <DatePicker SelectedDate="{Binding DateFrom}" 
                                Width="200" Height="50" FontSize="20" 
                                VerticalContentAlignment="Center"/>
                </StackPanel>

                <!-- Date To -->
                <StackPanel Grid.Column="2" Orientation="Vertical">
                    <TextBlock Text="To Date" Style="{StaticResource HmiLabelStyle}"/>
                    <DatePicker SelectedDate="{Binding DateTo}" 
                                Width="200" Height="50" FontSize="20" 
                                VerticalContentAlignment="Center"/>
                </StackPanel>

                <!-- Action Buttons -->
                <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button Content="LOAD DATA" 
                            Command="{Binding LoadDataCommand}"
                            Width="180" Height="50"
                            FontSize="20" FontWeight="Bold"
                            Background="#007ACC" Foreground="White"
                            BorderThickness="0" Margin="0,0,15,0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Button Content="EXPORT CSV" 
                            Command="{Binding ExportCsvCommand}"
                            Width="180" Height="50"
                            FontSize="20" FontWeight="Bold"
                            Background="#10B981" Foreground="White"
                            BorderThickness="0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 3. Data Grid -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="10" Margin="0,0,0,20">
            <!-- 
                 CRITICAL FIXES FOR HORIZONTAL SCROLLING:
                 1. ColumnWidth="Auto" (Forces columns to take necessary space instead of squishing)
                 2. MinColumnWidth="150" (Ensures columns don't shrink too small)
                 3. ScrollViewer.HorizontalScrollBarVisibility="Auto"
            -->
            <DataGrid ItemsSource="{Binding ReportDataTable}"
                      AutoGenerateColumns="True"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      RowHeight="50"
                      FontSize="18"
                      HeadersVisibility="Column"
                      Background="White"
                      BorderThickness="0"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#E5E7EB"
                      VerticalGridLinesBrush="Transparent"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ColumnWidth="Auto"
                      MinColumnWidth="150"
                      EnableColumnVirtualization="True"
                      EnableRowVirtualization="True"
                      AlternationCount="2">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="FontSize" Value="18"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Foreground" Value="#374151"/>
                        <Setter Property="Padding" Value="15,0"/>
                        <Setter Property="Background" Value="#F9FAFB"/>
                        <Setter Property="BorderBrush" Value="#E5E7EB"/>
                        <Setter Property="BorderThickness" Value="0,0,1,2"/>
                        <Setter Property="Height" Value="60"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="White"/>
                        <Style.Triggers>
                            <Trigger Property="AlternationIndex" Value="1">
                                <Setter Property="Background" Value="#F9FAFB"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#EBF5FF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#CCE8FF"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="15,0"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}" 
                                            BorderBrush="{TemplateBinding BorderBrush}" 
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.CellStyle>
            </DataGrid>
        </Border>

        <!-- 4. Footer Status Bar -->
        <Border Grid.Row="3" Background="White" CornerRadius="12" BorderBrush="#E5E7EB" BorderThickness="1" Padding="25,15">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="2" Opacity="0.05"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Selected Format Info -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="Selected Format: " FontSize="20" Foreground="#6B7280" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding SelectedReportFormat.Name, FallbackValue='None'}" 
                               FontSize="20" FontWeight="Bold" Foreground="#111827" VerticalAlignment="Center"/>

                    <TextBlock Text="  |  Columns: " FontSize="20" Foreground="#6B7280" VerticalAlignment="Center" Margin="20,0,0,0"/>
                    <TextBlock Text="{Binding SelectedFormatColumns.Count, FallbackValue='0'}" 
                               FontSize="20" FontWeight="Bold" Foreground="#111827" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Row Count -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="Total Rows: " FontSize="20" Foreground="#6B7280" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalRowsLoaded}" 
                               FontSize="22" FontWeight="Bold" Foreground="#007ACC" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>