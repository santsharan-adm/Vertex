<UserControl x:Class="IPCSoftware.App.Views.AlarmConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IPCSoftware.App.Views"
             mc:Ignorable="d" 
      
    d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" 
                   Text="{Binding Title}" 
                   FontSize="18" 
                   FontWeight="SemiBold"
                   Margin="0,0,0,20"/>

        <!-- Form -->
        <Border Grid.Row="1" 
                BorderBrush="#CCCCCC" 
                BorderThickness="1" 
                Padding="20"
                Background="White">

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>

                    <!-- Alarm No -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Alarm No" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" 
                                 Text="{Binding AlarmNo, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="25" 
                                 Padding="5,3"/>
                    </Grid>

                    <!-- Alarm Name -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Alarm Name" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" 
                                 Text="{Binding AlarmName, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="25" 
                                 Padding="5,3"/>
                    </Grid>

                    <!-- Tag No -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Tag No" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" 
                                 Text="{Binding TagNo, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="25" 
                                 Padding="5,3"/>
                    </Grid>

                    <!-- Name -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Name" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" 
                                 Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="25" 
                                 Padding="5,3"/>
                    </Grid>

                    <!-- Alarm Bit -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Alarm Bit" VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1" 
                                  ItemsSource="{Binding AlarmBits}"
                                  Text="{Binding AlarmBit}"
                                  IsEditable="True"
                                  Height="25"/>
                    </Grid>

                    <!-- Alarm Text -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Alarm Text" VerticalAlignment="Top" Margin="0,5,0,0"/>
                        <TextBox Grid.Column="1" 
                                 Text="{Binding AlarmText, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="60" 
                                 Padding="5,3" 
                                 TextWrapping="Wrap" 
                                 AcceptsReturn="True" 
                                 VerticalScrollBarVisibility="Auto"/>
                    </Grid>

                    <!-- Severity -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Severity" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" 
                                 Text="{Binding Severity, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="25" 
                                 Padding="5,3"/>
                    </Grid>

                    <!-- Alarm Time -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Alarm Time" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" 
                                 Text="{Binding AlarmTime, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="25" 
                                 Padding="5,3"
                                
                                 Background="#F0F0F0"/>
                    </Grid>

                    <!-- Alarm Reset Time -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Alarm Reset Time" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" 
                                 Text="{Binding AlarmResetTime, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="25" 
                                 Padding="5,3"
                                
                                 Background="#F0F0F0"/>
                    </Grid>

                    <!-- Alarm Ack Time -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Alarm Ack Time" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" 
                                 Text="{Binding AlarmAckTime, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="25" 
                                 Padding="5,3"
                                
                                 Background="#F0F0F0"/>
                    </Grid>

                    <!-- Description -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Description" VerticalAlignment="Top" Margin="0,5,0,0"/>
                        <TextBox Grid.Column="1" 
                                 Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="60" 
                                 Padding="5,3" 
                                 TextWrapping="Wrap" 
                                 AcceptsReturn="True" 
                                 VerticalScrollBarVisibility="Auto"/>
                    </Grid>

                    <!-- Remark -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Remark" VerticalAlignment="Top" Margin="0,5,0,0"/>
                        <TextBox Grid.Column="1" 
                                 Text="{Binding Remark, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="60" 
                                 Padding="5,3" 
                                 TextWrapping="Wrap" 
                                 AcceptsReturn="True" 
                                 VerticalScrollBarVisibility="Auto"/>
                    </Grid>

                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,15,0,0">
            <Button Content="Save" 
                    Command="{Binding SaveCommand}"
                    Width="100" 
                    Height="30" 
                    Margin="0,0,10,0"
                    Background="#007ACC"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="3"
                                            Padding="15,5">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <Button Content="Cancel" 
                    Command="{Binding CancelCommand}"
                    Width="100" 
                    Height="30"
                    Background="#F0F0F0"
                    BorderBrush="#CCCCCC"
                    BorderThickness="1"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="3"
                                            Padding="15,5">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
