<UserControl x:Class="IPCSoftware.App.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:IPCSoftware.Helpers;assembly=IPCSoftware.Helpers"
        Background="White">
    <Viewbox>
    <Grid>

        <!-- MAIN CARD -->
        <Border Width="800"
                Height="500"
                CornerRadius="5"
                Background="White"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <!--<Border.Effect>
                <DropShadowEffect BlurRadius="25"
                                  ShadowDepth="0"
                                  Color="#50000000"/>
            </Border.Effect>-->

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- LEFT BLUE PANEL -->
                <Border Grid.Column="0"
                        Background="#245CFF"
                        CornerRadius="0,0,0,0"
                        Padding="40">

                    <StackPanel VerticalAlignment="Center">

                        <TextBlock Text="Be Verified"
                                   FontSize="32"
                                   FontWeight="Bold"
                                   Foreground="White"
                                   HorizontalAlignment="Center"/>

                        <TextBlock Text="Login with your authorized credentials."
                                   FontSize="14"
                                   Foreground="#DDE6FF"
                                   Margin="0,15,0,0"
                                   TextAlignment="Center"/>

                    </StackPanel>
                </Border>

                <!-- RIGHT LOGIN FORM -->
                <Grid Grid.Column="1">

                    <Border Padding="40" Background="Transparent" Margin="0,0,5,0">

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- TITLE -->
                            <StackPanel Grid.Row="0" Margin="0,0,0,25">
                                    <TextBlock Text="Please login"
                                           FontSize="28"
                                           HorizontalAlignment="Center"
                                           FontWeight="Bold"
                                           Foreground="#222"/>

                                <!--<TextBlock Text="We are happy to have you back."
                                           FontSize="13"
                                           Foreground="Gray"
                                           Margin="0,8,0,0"/>-->
                            </StackPanel>

                            <!-- USERNAME -->
                            <Border Grid.Row="1"
                                    Height="45"
                                    CornerRadius="5"
                                    Background="#F2F4F8"
                                    Padding="10"
                                    Margin="0,0,0,15">

                                <TextBox Text="{Binding Username}"
                                         FontSize="14"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         VerticalContentAlignment="Center"
                                         Foreground="Black"
                                         ToolTip="Username" TextChanged="TextBox_TextChanged_1"/>
                            </Border>

                            <!-- PASSWORD -->
                            <Border Grid.Row="2"
                                    Height="45"
                                    CornerRadius="10"
                                    Background="#F2F4F8"
                                    Padding="10"
                                    Margin="0,0,0,25">

                                <PasswordBox FontSize="14"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             VerticalContentAlignment="Center"
                                             helpers:PasswordBoxHelper.BindPassword="True"
                                             helpers:PasswordBoxHelper.BindablePassword="{Binding Password, Mode=TwoWay}"/>
                            </Border>

                            <!-- LOGIN BUTTON -->
                            <Button Grid.Row="3"
                                    Content="Login"
                                    Command="{Binding LoginCommand}"
                                    Height="45"
                                    FontSize="16"
                                    Foreground="White"
                                    Background="#245CFF"
                                    BorderThickness="0"
                                    Cursor="Hand">

                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="12">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                        </Border>

                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#1C4FE0"/>
                                            </Trigger>

                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="Background" Value="#143BB7"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>

                            </Button>

                        </Grid>
                    </Border>
                </Grid>

            </Grid>
        </Border>

    </Grid>
    </Viewbox>
</UserControl>