<UserControl x:Class="IPCSoftware.App.Views.ReportViewerView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
 mc:Ignorable="d"
 d:DesignHeight="800" d:DesignWidth="1400"
 Background="#F3F3F3">
 <Grid Margin="40">
 <Grid.RowDefinitions>
 <RowDefinition Height="Auto"/>
 <RowDefinition Height="Auto"/>
 <RowDefinition Height="*"/>
 </Grid.RowDefinitions>
 <Grid.ColumnDefinitions>
 <ColumnDefinition Width="2*"/>
 <ColumnDefinition Width="2*"/>
 <ColumnDefinition Width="4*"/>
 </Grid.ColumnDefinitions>

 <!-- Report Format Selection -->
 <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Vertical" Margin="0,0,20,0">
 <TextBlock Text="Select Report Format:" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
 <ComboBox ItemsSource="{Binding ReportFormats}"
 SelectedItem="{Binding SelectedReportFormat}"
 DisplayMemberPath="Name"
 Width="250" Height="32"/>
 </StackPanel>

 <!-- Date Range Selection -->
 <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,20,0">
 <TextBlock Text="From:" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
 <DatePicker SelectedDate="{Binding DateFrom}" Width="140" Margin="0,0,20,0"/>
 <TextBlock Text="To:" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
 <DatePicker SelectedDate="{Binding DateTo}" Width="140"/>
 </StackPanel>

 <!-- Buttons: Load Data, Export CSV -->
 <StackPanel Grid.Row="0" Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,0">
 <Button Content="Load Data" Command="{Binding LoadDataCommand}"
 Width="120" Height="36" Margin="0,0,10,0"
 Background="#1976D2" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
 <Button Content="Export CSV" Command="{Binding ExportCsvCommand}"
 Width="120" Height="36"
 Background="#388E3C" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
 </StackPanel>

 <!-- Data Grid -->
 <DataGrid Grid.Row="2" Grid.ColumnSpan="3"
 ItemsSource="{Binding ReportDataTable}"
 AutoGenerateColumns="True"
 CanUserAddRows="False"
 IsReadOnly="True"
 Margin="0,30,0,0"
 RowHeight="40"
 FontSize="16"
 HeadersVisibility="Column"
 Background="White"
 BorderBrush="#DDD"
 BorderThickness="1"/>
 </Grid>
</UserControl>
