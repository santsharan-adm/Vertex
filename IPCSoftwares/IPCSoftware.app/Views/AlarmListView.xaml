<UserControl x:Class="IPCSoftware.App.Views.AlarmListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IPCSoftware.App.Views"
             xmlns:helpers="clr-namespace:IPCSoftware.App.Helpers"
             xmlns:vm="clr-namespace:IPCSoftware.App.ViewModels"
             helpers:ViewModelLocator.AutoWireViewModel="{x:Type vm:AlarmListViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920"
             Background="#F3F3F3" >

    <UserControl.Resources>
        <!-- ================= LARGE HMI DATAGRID STYLES ================= -->

        <Style x:Key="HmiColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Height" Value="90"/>
            <Setter Property="FontSize" Value="32"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
        </Style>

        <Style x:Key="HmiCellStyle" TargetType="DataGridCell">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                SnapsToDevicePixels="True">
                            <ContentPresenter VerticalAlignment="Center" 
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#CCE8FF"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CenterAlignedCellStyle" TargetType="DataGridCell" BasedOn="{StaticResource HmiCellStyle}">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="LeftAlignedCellStyle" TargetType="DataGridCell" BasedOn="{StaticResource HmiCellStyle}">
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="20,0,0,0"/>
        </Style>

        <!-- Filter Box Style -->
        <Style x:Key="HmiFilterTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="70"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="2"/>
        </Style>

    </UserControl.Resources>

    <!-- Scale Container -->
    <Viewbox Stretch="Uniform">
        <Grid Width="1920" Height="1080" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 1. HEADER (Filter & Add Button) -->
            <Grid Grid.Row="0" Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <!-- Filter Label -->
                    <ColumnDefinition Width="600"/>
                    <!-- Filter Input -->
                    <ColumnDefinition Width="*"/>
                    <!-- Spacer -->
                    <ColumnDefinition Width="Auto"/>
                    <!-- Add Button -->
                </Grid.ColumnDefinitions>

                <!-- Filter Label -->
                <TextBlock Grid.Column="0" 
                           Text="Filter:" 
                           FontSize="36" 
                           FontWeight="SemiBold" 
                           VerticalAlignment="Center"
                           Margin="0,0,20,0"
                           Foreground="#555555"/>

                <!-- Filter Input -->
                <TextBox Grid.Column="1"
                         Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource HmiFilterTextBox}"
                         Tag="Search alarms..."/>

                <!-- Add Button -->
                <Button Grid.Column="3" 
                        Content="+ ADD ALARM" 
                        Command="{Binding AddAlarmCommand}"
                        Height="90" Width="350"
                        FontSize="32" FontWeight="Bold"
                        Background="#28A745"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>

            <!-- 2. DATA GRID -->
            <Border Grid.Row="1" 
                    Background="White" 
                    CornerRadius="12" 
                    BorderBrush="#CCCCCC" 
                    BorderThickness="2">

                <DataGrid ItemsSource="{Binding FilteredAlarms}"
                          SelectedItem="{Binding SelectedAlarm}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserReorderColumns="False"
                          CanUserResizeColumns="False"
                          CanUserResizeRows="False"
                          CanUserSortColumns="True"
                          
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#E0E0E0"
                          VerticalGridLinesBrush="Transparent"
                          BorderThickness="0"
                          Background="White"
                          
                          RowHeight="100" 
                          ColumnHeaderStyle="{StaticResource HmiColumnHeaderStyle}"
                          HeadersVisibility="Column">

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="AlternationIndex" Value="1">
                                    <Setter Property="Background" Value="#FAFAFA"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F0F0F0"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#E6F2FF"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.AlternationCount>2</DataGrid.AlternationCount>

                    <DataGrid.Columns>
                        <!-- ID (Small, Centered) -->
                        <!--<DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="100" CellStyle="{StaticResource CenterAlignedCellStyle}"/>-->

                        <!-- Alarm No (Centered) -->
                        <DataGridTextColumn Header="No." Binding="{Binding AlarmNo}" Width="120" CellStyle="{StaticResource CenterAlignedCellStyle}"/>

                        <!-- Alarm Name (Left) -->
                        <DataGridTextColumn Header="Name" Binding="{Binding AlarmName}" Width="250" CellStyle="{StaticResource LeftAlignedCellStyle}"/>

                        <!-- Alarm Text (Fills Remaining Space) -->
                        <DataGridTextColumn Header="Tag No" Binding="{Binding TagNo}" Width="200" CellStyle="{StaticResource CenterAlignedCellStyle}"/>

                        <!-- Severity (Centered) -->
                        <DataGridTextColumn Header="Severity" Binding="{Binding Severity}"  CellStyle="{StaticResource CenterAlignedCellStyle}"/>
                        <DataGridTextColumn Header="Alarm Bit" Binding="{Binding AlarmBit}" Width="220" CellStyle="{StaticResource CenterAlignedCellStyle}"/>
                        <DataGridTextColumn Header="Alarm Text" Binding="{Binding AlarmText}" Width="*" CellStyle="{StaticResource LeftAlignedCellStyle}"/>
                        <!-- Times (Fixed Width, Centered) -->
                        <!--<DataGridTextColumn Header="Alarm Time" Binding="{Binding AlarmTime, StringFormat='dd-MM HH:mm'}" Width="220" CellStyle="{StaticResource CenterAlignedCellStyle}"/>
                        <DataGridTextColumn Header="Reset Time" Binding="{Binding AlarmResetTime, StringFormat='dd-MM HH:mm'}" Width="220" CellStyle="{StaticResource CenterAlignedCellStyle}"/>
                        <DataGridTextColumn Header="Ack Time" Binding="{Binding AlarmAckTime, StringFormat='dd-MM HH:mm'}" Width="220" CellStyle="{StaticResource CenterAlignedCellStyle}"/>-->

                        <!-- Actions (Large Buttons) -->
                        <DataGridTemplateColumn Header="Actions" Width="360">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <!-- Edit Button -->
                                        <Button Content="Edit" 
                                                Command="{Binding DataContext.EditAlarmCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Width="140" Height="70"
                                                Background="#007ACC" Foreground="White"
                                                FontSize="24" FontWeight="Bold"
                                                BorderThickness="0" Margin="0,0,20,0">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="8"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>

                                        <!-- Delete Button -->
                                        <Button Content="Del" 
                                                Command="{Binding DataContext.DeleteAlarmCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Width="140" Height="70"
                                                Background="#DC3545" Foreground="White"
                                                FontSize="24" FontWeight="Bold"
                                                BorderThickness="0">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="8"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Viewbox>
</UserControl>