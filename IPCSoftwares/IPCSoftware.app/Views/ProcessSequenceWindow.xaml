<Window x:Class="IPCSoftware.App.Views.ProcessSequenceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Process Sequence Monitor"
        WindowStartupLocation="CenterOwner"
        Width="900" Height="600"
        Background="#1E1E1E">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" VerticalAlignment="Center" Margin="0,0,0,10">
            <TextBlock Text="Live Sequence" FontSize="24" FontWeight="Bold" Foreground="White" />
            <Button Content="Clear" Command="{Binding ClearCommand}" Margin="20,0,0,0" Padding="16,6" />
        </StackPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Logs}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Background="#232323"
                  Foreground="White"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  BorderThickness="0"
                  RowBackground="#2D2D30"
                  AlternatingRowBackground="#333337">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding Step}" Width="60"/>
                <DataGridTextColumn Header="Time" Binding="{Binding TimeDisplay}" Width="130"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
