<UserControl x:Class="IPCSoftware.App.Views.PLCIOMonitor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IPCSoftware.App.Views"
              xmlns:vm="clr-namespace:IPCSoftware.App.ViewModels"
             xmlns:conv="clr-namespace:IPCSoftware.App.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:PlcIoMonitorViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <conv:OnOffToBrushConverter x:Key="OnOffBrush"/>
    </UserControl.Resources>

    <Border Margin="20" Padding="10" BorderBrush="Black" BorderThickness="1">

        <StackPanel>

            <TextBlock Text="PLC IO Monitor"
                       FontSize="16"
                       FontWeight="Bold"
                       Margin="0 0 0 10"/>

            <DataGrid ItemsSource="{Binding IoItems}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      RowBackground="White"
                      AlternatingRowBackground="White"
                      MouseDoubleClick="DataGrid_MouseDoubleClick">

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background"
                                Value="{Binding Value, Converter={StaticResource OnOffBrush}}"/>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>

                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>

                    <DataGridTextColumn Header="PLC Tag" Binding="{Binding Tag}" Width="150"/>

                    <DataGridTemplateColumn Header="Value" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Value}"
                                        Command="{Binding DataContext.ToggleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Width="70"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Description"
                                        Binding="{Binding Description}"
                                        Width="200"/>

                </DataGrid.Columns>

            </DataGrid>
        </StackPanel>

    </Border>
</UserControl>
