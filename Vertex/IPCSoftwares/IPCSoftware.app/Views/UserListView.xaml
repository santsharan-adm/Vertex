<UserControl x:Class="IPCSoftware.App.Views.UserListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:IPCSoftware.App.ViewModels"
             xmlns:helpers="clr-namespace:IPCSoftware.App.Helpers"
             xmlns:converters="clr-namespace:IPCSoftware.App.Converters"
             helpers:ViewModelLocator.AutoWireViewModel="{x:Type vm:UserListViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920"
             Background="#F3F3F3" >

    <UserControl.Resources>
        <converters:BooleanToYesNoConverter x:Key="BoolToYesNo"/>

        <!-- ================= LARGE HMI DATAGRID STYLES ================= -->

        <Style x:Key="HmiColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Height" Value="90"/>
            <Setter Property="FontSize" Value="36"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="15,0"/>
        </Style>

        <Style x:Key="HmiCellStyle" TargetType="DataGridCell">
            <Setter Property="FontSize" Value="32"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                SnapsToDevicePixels="True">
                            <ContentPresenter VerticalAlignment="Center" 
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#CCE8FF"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CenterAlignedCellStyle" TargetType="DataGridCell" BasedOn="{StaticResource HmiCellStyle}">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="LeftAlignedCellStyle" TargetType="DataGridCell" BasedOn="{StaticResource HmiCellStyle}">
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="75,0,0,0"/>
        </Style>

        <!-- Filter Box Style -->
        <Style x:Key="HmiFilterTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="70"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="2"/>
        </Style>

    </UserControl.Resources>

    <!-- Scale Container -->
    <Viewbox Stretch="Uniform">
        <Grid Width="1920" Height="1080" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 1. HEADER (Title, Search, Add Button) -->
            <Grid Grid.Row="0" Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <!-- Title -->
                    <ColumnDefinition Width="50"/>
                    <!-- Spacer -->
                    <ColumnDefinition Width="600"/>
                    <!-- Search Input -->
                    <ColumnDefinition Width="*"/>
                    <!-- Spacer -->
                    <ColumnDefinition Width="Auto"/>
                    <!-- Add Button -->
                </Grid.ColumnDefinitions>

                <!-- Title -->
                <TextBlock Grid.Column="0" 
                           Text="User Management" 
                           FontSize="60" 
                           FontWeight="Bold"
                           Foreground="#333333"
                           VerticalAlignment="Center"/>

                <!-- Search Input -->
                <Grid Grid.Column="2" VerticalAlignment="Center">
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                          >
                        <TextBox.Style>
                            <Style TargetType="TextBox" BasedOn="{StaticResource HmiFilterTextBox}">
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <VisualBrush Stretch="None" AlignmentX="Left">
                                                    <VisualBrush.Visual>
                                                        <TextBlock FontStyle="Italic" Text="Search User..." 
                                                                   FontSize="28"
                                                                   Foreground="#999999" 
                                                                   Margin="15,0,0,0"
                                                                   />
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </Grid>

                <!-- Add Button -->
                <Button Grid.Column="4" 
                        Content="+ ADD USER" 
                        Command="{Binding AddUserCommand}"
                        Height="90" Width="350"
                        FontSize="32" FontWeight="Bold"
                        Background="#007ACC"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>

            <!-- 2. DATA GRID -->
            <Border Grid.Row="1" 
                    Background="White" 
                    CornerRadius="12" 
                    BorderBrush="#CCCCCC" 
                    BorderThickness="2">

                <DataGrid ItemsSource="{Binding FilteredUsers}"
                          SelectedItem="{Binding SelectedUser}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserReorderColumns="False"
                          CanUserResizeColumns="False"
                          CanUserResizeRows="False"
                          CanUserSortColumns="True"
                          
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#E0E0E0"
                          VerticalGridLinesBrush="Transparent"
                          BorderThickness="0"
                          Background="White"
                          
                          RowHeight="100" 
                          ColumnHeaderStyle="{StaticResource HmiColumnHeaderStyle}"
                          HeadersVisibility="Column">

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="AlternationIndex" Value="1">
                                    <Setter Property="Background" Value="#FAFAFA"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F0F0F0"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#E6F2FF"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.AlternationCount>2</DataGrid.AlternationCount>

                    <DataGrid.InputBindings>
                        <MouseBinding MouseAction="LeftDoubleClick" 
                                      Command="{Binding EditUserCommand}" 
                                      CommandParameter="{Binding SelectedUser}"/>
                    </DataGrid.InputBindings>

                    <DataGrid.Columns>
                        <!-- First Name -->
                        <DataGridTextColumn  Header="First Name" Binding="{Binding FirstName}" Width="300" CellStyle="{StaticResource LeftAlignedCellStyle}"/>

                        <!-- Last Name -->
                        <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="300" CellStyle="{StaticResource LeftAlignedCellStyle}"/>

                        <!-- User Name (Fills space) -->
                        <DataGridTextColumn Header="User Name" Binding="{Binding UserName}" Width="*" CellStyle="{StaticResource LeftAlignedCellStyle}"/>

                        <!-- Role (Centered) -->
                        <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="250" CellStyle="{StaticResource CenterAlignedCellStyle}"/>

                        <!-- Active Status (Centered) -->
                        <DataGridTextColumn Header="Active" Binding="{Binding IsActive, Converter={StaticResource BoolToYesNo}}" Width="200" CellStyle="{StaticResource CenterAlignedCellStyle}"/>

                        <!-- Actions (Large Buttons) -->
                        <DataGridTemplateColumn Header="Actions" Width="400">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <!-- Edit Button -->
                                        <Button Content="Edit" 
                                                Command="{Binding DataContext.EditUserCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Width="160" Height="70"
                                                Background="#007ACC" Foreground="White"
                                                FontSize="24" FontWeight="Bold"
                                                BorderThickness="0" Margin="0,0,20,0">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="8"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>

                                        <!-- Delete Button -->
                                        <Button Content="Delete" 
                                                Command="{Binding DataContext.DeleteUserCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Width="160" Height="70"
                                                Background="#DC3545" Foreground="White"
                                                FontSize="24" FontWeight="Bold"
                                                BorderThickness="0">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="8"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Viewbox>
</UserControl>